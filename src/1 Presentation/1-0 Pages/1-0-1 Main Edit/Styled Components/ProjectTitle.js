import styled from 'styled-components';

import responsive from '../../../../2 helper functions/responsive';

export const ProjectTitle = styled.div`
  margin-left: 4vw;
  font-size: ${responsive("biggest", 1920, 3, 5)}vmax;
  font-family: "Righteous";
  color: hsl(30, 80%, 60%);
`